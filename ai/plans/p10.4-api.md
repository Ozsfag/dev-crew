# П10.4 — API: Полнота и производительность

## Контекст

List-эндпоинты не имеют пагинации, отсутствует OpenAPI-документация, нет real-time обновлений статуса задач.

---

## 4.1 Пагинация на list-эндпоинтах

**Файлы**: контроллеры `task`, `audit`, `billing`

**Проблема**: `GET /api/tasks`, `GET /api/audit`, `GET /api/billing/usage` возвращают
все записи без пагинации. При росте данных — OutOfMemory и медленные ответы.

**Решение**:
```java
@GetMapping
public Page<TaskResponse> list(
    @PageableDefault(size = 20, sort = "createdAt", direction = DESC) Pageable pageable,
    @AuthenticationPrincipal AuthenticatedUser principal) {
  return taskQueryService.getByOrg(principal.orgId(), pageable)
      .map(taskWebMapper::toResponse);
}
```

**Acceptance Criteria**:
- [x] `GET /api/tasks?page=0&size=20` работает (PageResult, сортировка по createdAt DESC)
- [x] Без параметров: дефолт 20 записей, сортировка по createdAt DESC
- [x] Тест: ответ содержит поля `content`, `totalElements`, `page`, `size`

---

## 4.2 OpenAPI документация — ОТЛОЖЕНО

Отложено как отдельная задача. Требует добавления springdoc-openapi и `@Operation` аннотаций на все контроллеры.

---

## 4.3 SSE endpoints — ОТЛОЖЕНО

Отложено как отдельная задача. Требует SseEmitter/ApplicationEventPublisher и lifecycle management.

---

## Тест-план

```bash
./gradlew spotlessApply
./gradlew test
```

## Зависимости

- 4.1 зависит от П10.1 (фильтрация по org)
