<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Локальная разработка: читаемый формат с цветами -->
    <springProfile name="!prod">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- Продакшн: структурированные JSON-логи (ECS) через Spring Boot 3.4+ native support -->
    <!-- Активируется при spring.profiles.active=prod -->
    <!-- logging.structured.format.console=ecs задаётся в application-prod.yml -->
    <springProfile name="prod">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
